# Write your MySQL query statement below
select p1.product_id, ifnull(round(sum(p1.price*p2.units)/sum(p2.units),2),0) as average_price from prices p1 left join unitssold p2 on p1.product_id=p2.product_id and p2.purchase_date between p1.start_date and p1.end_date group by p1.product_id
